{
  "name": "new2",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo Enter your bot token : && read token && echo Enter Admin Password : && read pass && node install.mjs $token $pass",
    "start": "node index.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "node-fetch": "^3.1.1",
    "telegram-bot-api": "^1.3.3"
  }
}
